pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //Add the JitPack repository to use package repository for Git
        //Publish JVM and Android libraries
        maven {
            url "https://jitpack.io"
        }
    }
}
rootProject.name = "My Application"
include ':app'
